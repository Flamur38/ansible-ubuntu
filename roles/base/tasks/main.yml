- name: Update apt cache
  apt:
    update_cache: yes

- name: Upgrade system
  apt:
    upgrade: dist

- name: Install essential packages
  apt:
    name:
      - curl
      - wget
      - git
      - ca-certificates
      - build-essential
      - unzip
    state: present

